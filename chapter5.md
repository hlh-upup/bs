# 第5章 面向教学视频制作的数字人生成系统设计与应用验证

在前述章节中，第3章完成了说话人脸视频五维质量评价模型的构建，第4章对评价模型及相关系统能力进行了实验验证。本章进一步从应用落地角度出发，面向高校与职业教育场景中的教学视频制作需求，构建并分析一套集成 SadTalker 与 Wav2Lip 的双路线数字人生成系统。与单一模型演示不同，本章强调“场景驱动的系统组织方式”：以**新课程制作**与**课程素材复用/更新**两类典型教学任务为牵引，将异构生成技术纳入统一流程，并通过嵌入第3章评价模型形成“生成—评价—优化”闭环。

本章研究目标包括三点：

1. 依据教学内容生产链路，建立“图片驱动 + 视频驱动”双技术路线的系统化实现；
2. 构建统一任务编排机制，实现 SadTalker 与 Wav2Lip 在同一平台下的协同运行；
3. 将第3章五维质量评价框架嵌入系统后处理阶段，验证其对异构生成路线的泛化适应能力。

说明：本章“图5.1—图5.7”当前采用 ASCII 示意占位，仅用于论文写作阶段的结构表达，定稿时将统一替换为规范插图。

## 5.1 教学视频制作场景需求分析

### 5.1.1 场景一：新课程制作（图片驱动）

在新课程开发初期，教师通常仅具备讲师头像、课程 PPT 及讲稿文本，缺少可直接复用的视频素材。该类任务对系统的核心要求是：能够从静态人像出发，结合文本与音频生成具有可接受自然度的讲解视频，并支持后续按章节批量生成。SadTalker 的人像驱动机制与该需求高度匹配，因此本研究将其定义为场景一的主技术路线。

### 5.1.2 场景二：课程素材复用/更新（视频驱动）

在教学迭代阶段，已有课程常需要“小步快跑式更新”，例如替换部分知识点、修订术语或补录案例说明。此时通常已有讲师视频片段，关键需求转向“尽量保持原视频视觉风格前提下，快速完成语音内容更新与口型重同步”。Wav2Lip 在口型对齐方面具有优势，适于该类视频驱动编辑任务，因此被定义为场景二主技术路线。

### 5.1.3 双场景统一建模

尽管两类场景在输入形态与生成机制上存在差异，但其业务目标一致：以较低制作成本形成可发布教学视频。基于此，本研究采用“前端统一入口 + 后端分支执行 + 统一质量评价”的系统策略，在统一平台中完成异构模型组织。

> **【图5.1 教学视频双场景需求与技术映射图（占位）】**
>
> 图示说明（ASCII 占位，后续替换为正式图）：
>
> ```text
> 教学视频生产需求
>      |
>      +--------------------+--------------------+
>      |                                         |
> 新课程制作（仅照片/PPT）                素材复用更新（已有视频）
>      |                                         |
> SadTalker 路线                           Wav2Lip 路线
>      +--------------------+--------------------+
>                           |
>                   统一质量评价与优化反馈
> ```

## 5.2 双路线数字人生成系统总体设计

### 5.2.1 系统架构

系统采用前后端分离架构。前端负责教学素材上传、路线选择与参数配置；后端负责模型调度、异步执行与结果管理。前端项目为 `digital-human-vue`，后端项目为 `Digital_Human_API-main`。系统通过统一 API 协议实现状态查询与结果回传，从而将多模型执行过程抽象为一致的任务流程。

> **【图5.2 系统总体架构图（占位）】**
>
> 图示说明（ASCII 占位，后续替换为正式图）：
>
> ```text
> +---------------------+      HTTP/JSON      +---------------------------+
> | 前端交互层(Vue3)    | <-----------------> | 后端服务层(Flask)         |
> | 素材上传/参数配置    |                     | 任务编排/状态管理/接口返回 |
> +----------+----------+                     +------------+--------------+
>            |                                               |
>            |                                        模型推理执行层
>            |                                               |
>            |                              +----------------v----------------+
>            |                              | SadTalker / Wav2Lip / VITS      |
>            |                              | 音视频中间件与结果文件管理       |
>            |                              +---------------------------------+
> ```

### 5.2.2 统一任务编排机制

前端状态层将路线选择抽象为统一配置变量（如动作模式、音频来源、插入方式等），后端以任务接口接收配置后进行分支调度。该机制的意义在于：

- 对上层用户隐藏模型差异，降低系统使用门槛。
- 对下层推理过程提供统一状态轮询能力，便于工程稳定性控制。
- 为后续接入更多生成模型预留扩展接口。

### 5.2.3 异步执行与状态回传

教学视频生成涉及多阶段耗时任务（音频生成、逐页视频推理、合成与导出），系统采用异步执行并通过状态接口轮询任务进度，实现“提交即返回、后台执行、前端可视追踪”的交互模式。该机制可避免同步阻塞造成的用户等待失控问题，提高平台可用性。

## 5.3 双技术路线的教学应用实现

### 5.3.1 SadTalker 路线：面向新课程制作的图片驱动生成

SadTalker 路线的核心流程为“静态人像 + 语音驱动 + 逐页视频生成”。在本系统中，PPT 备注文本首先用于语音生成（或接入用户音频），随后驱动人像完成逐页讲解视频片段生成，最终进入课件视频合成阶段。该路线适合“从无到有”的课程构建任务。

> **【图5.3 SadTalker 路线流程图（占位）】**
>
> 图示说明（ASCII 占位，后续替换为正式图）：
>
> ```text
> 讲师照片 + PPT备注文本
>          |
>       语音生成/导入
>          |
>       SadTalker逐页推理
>          |
>      讲解片段集合 + PPT合成
>          |
>        新课程视频输出
> ```

### 5.3.2 Wav2Lip 路线：面向素材复用更新的视频驱动编辑

Wav2Lip 路线的核心流程为“现有视频 + 更新语音 + 口型重同步”。系统在保持原有视频主体风格的基础上，对更新后的语音内容执行对口型推理，适用于教学内容局部更新与版本迭代。该路线可显著降低“重新拍摄”成本。

> **【图5.4 Wav2Lip 路线流程图（占位）】**
>
> 图示说明（ASCII 占位，后续替换为正式图）：
>
> ```text
> 既有教师视频 + 新语音内容
>            |
>       预处理与片段对齐
>            |
>         Wav2Lip口型同步
>            |
>       更新课节视频输出
> ```

### 5.3.3 双路线协同价值分析

SadTalker 与 Wav2Lip 分别覆盖“新增内容生产”与“存量内容更新”两个高频教学需求，二者并非替代关系，而是互补关系。系统将其并行纳入统一平台后，教学团队可根据素材条件选择最优路径，形成从课程初建到持续迭代的完整生产能力。

## 5.4 “生成—评价—优化”闭环构建

### 5.4.1 闭环机制设计

为避免“只生成、不评价”的粗放式内容生产，本研究将第3章五维质量评价框架嵌入系统后处理环节。每次生成任务完成后，系统进入质量评测阶段，输出结构化结果并反馈优化建议，进而指导下一轮生成参数调整。

> **【图5.5 生成—评价—优化闭环机制图（占位）】**
>
> 图示说明（ASCII 占位，后续替换为正式图）：
>
> ```text
> [教学素材输入] -> [SadTalker/Wav2Lip生成] -> [五维质量评价]
>         ^                                         |
>         |                                         v
> [参数更新与素材修订] <- [质量报告与优化建议输出]
> ```

### 5.4.2 五维评价嵌入方式

系统评价维度与第3章保持一致，包括：

1. 口型同步质量。
2. 画面清晰度。
3. 时序稳定性。
4. 音频质量。
5. 表情自然度。

通过统一评价接口，系统可对 SadTalker 与 Wav2Lip 两类结果采用相同指标体系进行评估，实现跨路线可比性。该过程可分为三个层次：当前状态上，系统已实现“生成任务完成后自动进入评测并返回结构化五维结果”的接口级闭环；实验基础上，第3章最优评价模型已在离线实验中完成有效性验证；部署路径上，后续将该模型替换接入在线评测引擎，完成从流程联调到实测部署的迁移。

### 5.4.3 优化反馈策略

质量报告除给出综合分级外，还针对薄弱维度给出定向建议。例如，在口型同步偏低时，可优先采用更高信噪比语音样本、减少背景噪声，并调整音频来源策略（模型音频/用户音频）；在时序稳定性不足时，可降低表情驱动幅度（如 `expression_scale`）、统一输出帧率（如固定到 25fps）并启用画面增强选项（如 `enhancer`）。该反馈策略使系统从“结果展示工具”转变为“生产决策辅助工具”。

> **【图5.6 五维评价结果展示与建议映射图（占位）】**
>
> 图示说明（ASCII 占位，后续替换为正式图）：
>
> ```text
> 维度分数：lip / visual / temporal / audio / expression
>      |
>  诊断规则映射
>      |
>  优化建议：音频处理/参数调整/素材替换/二次生成
> ```

## 5.5 异构路线下评价框架泛化适应能力讨论

本章的关键学术意义在于：通过在同一平台中同时接入图片驱动（SadTalker）与视频驱动（Wav2Lip）两类机制，并采用同一五维评价框架进行质量分析，验证了评价体系对异构生成技术的适应性。其主要体现如下：

1. **输入异构适应性**：可同时处理“静态图像起步”与“动态视频起步”两类生成任务。
2. **过程异构适应性**：可评价不同中间流程（音频驱动人像、视频口型重同步）所得结果。
3. **结果异构适应性**：在统一指标空间中对不同路线输出进行可比评估。
4. **决策一致性**：优化建议可回流到统一配置层，形成一致的迭代路径。

> **【图5.7 评价框架对异构技术路线的泛化适应示意图（占位）】**
>
> 图示说明（ASCII 占位，后续替换为正式图）：
>
> ```text
> SadTalker输出 ----+
>                   +--> 统一五维评价模型 --> 可比质量报告 --> 统一优化策略
> Wav2Lip输出  -----+
> ```

上述结果说明，第3章提出的评价框架并非依赖某一特定生成模型，而是具备跨技术路线迁移应用的潜力，为后续扩展到更多生成范式（如3D数字人、扩散式说话人脸模型）奠定了方法基础。

## 5.6 本章小结

本章面向教学视频制作场景，构建了集成 SadTalker（图片驱动）与 Wav2Lip（视频驱动）的双路线数字人生成系统，并将第3章五维质量评价框架嵌入系统流程，形成“生成—评价—优化”的应用闭环。研究表明，该闭环能够同时支持新课程制作与存量课程更新两类任务，并在异构技术路线下保持评价标准的一致性与可比性，从而验证了质量评价框架的泛化适应能力。

至此，论文完成了从“评价方法提出”到“系统级应用验证”的关键衔接，为第6章总结与展望提供了实践基础。
