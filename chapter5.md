# 第5章 数字人生成系统的工程实现与应用闭环

本章在前文算法研究与实验验证的基础上，进一步面向“可落地的数字人生产系统”展开论述。与第3章偏重质量评估算法、第4章偏重评估系统测试不同，本章聚焦于本研究实际构建的前后端一体化数字人生成系统，重点回答两个问题：

1. 如何将 `Wav2Lip` 与 `SadTalker` 组织为可切换、可扩展的双场景生成能力；
2. 如何把“生成—评估—优化”串成工程闭环，支撑论文中的应用价值。

系统代码由后端 `Digital_Human_API-main` 与前端 `digital-human-vue` 组成。后端基于 Flask 提供任务接口，前端基于 Vue3 + TypeScript 实现交互流程与结果展示。

## 5.1 系统应用定位与双场景设计

从业务目标看，本系统并非单一模型演示，而是面向不同生成需求提供两条技术路径：

- **场景A：Wav2Lip驱动的视频口型对齐场景**（偏“口型精度”和“讲话节奏一致性”）；
- **场景B：SadTalker驱动的虚拟人像讲解场景**（偏“人脸表情自然度”和“姿态表现力”）。

两条路径共享同一套输入管理、任务状态管理、视频合成与质量评测接口，体现“多模型能力复用 + 统一工程编排”的系统设计思想。

> **【图5-1 系统双场景定位示意图（占位）】**
>
> 图示说明（ASCII 占位，后续可替换为正式图）：
>
> ```text
>                +-----------------------------+
>                |      统一前端配置入口       |
>                |  (图片/PPT/音频/参数配置)   |
>                +--------------+--------------+
>                               |
>                     任务分流与参数路由
>                +--------------+--------------+
>                |                             |
>      +---------v----------+       +----------v---------+
>      |  场景A: Wav2Lip    |       |  场景B: SadTalker  |
>      |  强口型同步        |       |  强表情与姿态       |
>      +---------+----------+       +----------+---------+
>                |                             |
>                +--------------+--------------+
>                               |
>                    统一质量评测与反馈优化
> ```

## 5.2 系统总体架构与模块协同

系统采用前后端分离架构。前端负责配置输入、触发任务与可视化结果；后端负责模型调度、异步推理与文件组织。

> **【图5-2 前后端总体架构图（占位）】**
>
> 图示说明（ASCII 占位，后续可替换为正式图）：
>
> ```text
> +---------------------+      HTTP/JSON       +---------------------------+
> |  Vue3 前端          | <------------------> | Flask 后端 API            |
> | VideoConfigView     |                      | /Get_Inference            |
> | VideoListView       |                      | /Get_State                |
> | Pinia Store         |                      | /Get_Video_Quality_Eval   |
> +----------+----------+                      +------------+--------------+
>            |                                                |
>            |                                        模型与任务执行
>            |                                                |
>            |                                   +------------v-------------+
>            |                                   | VITS / Wav2Lip /SadTalker|
>            |                                   | 文件JSON与结果目录管理    |
>            |                                   +--------------------------+
> ```

### 5.2.1 前端编排层

前端路由由 `router/index.ts` 管理，核心业务页面包括：

- `PersonManagerView`：数字人形象管理；
- `VideoConfigView`：生成参数配置与任务触发；
- `VoiceTrainerView`：语音训练与模型选择；
- `VideoListView`：视频列表查看、下载与管理。

前端状态中将生成方式抽象为三组关键变量：

- `digitalMotion`：`1`（SadTalker）或 `2`（Wav2Lip）（该编号与前端 Store 中的枚举约定一致）；
- `intoDigitalOperation`：全插入 / 不插入 / 部分插入；
- `useModelAudio`：模型生成音频或用户上传音频。

基于该抽象，`generateDigitalHumanAdvanced` 可以在同一入口下分派不同后端分支。

### 5.2.2 后端服务层

后端通过 `/Get_Inference` 统一接收推理请求，并根据用户目录内音频条件自动决策：

- 存在 `User_Wav.wav`：走“用户音频 + Wav2Lip”；
- 否则若存在 `Ref_Wav.wav`：走“VITS + SadTalker”；
- 若两者都缺失：采用默认“VITS + SadTalker”分支。

任务提交后，服务立即返回任务标识，前端通过 `/Get_State` 轮询状态（如 `Processing` / `True` / `Failed`）。后端采用线程池异步执行，避免推理阻塞接口请求。

## 5.3 双场景生成流程设计

### 5.3.1 场景A：Wav2Lip 口型驱动流程

Wav2Lip 场景适用于“教师讲解视频”“口播视频修复”等强调唇形同步的任务。该流程的核心特征是先将视频统一为目标帧率，再按每页音频片段进行局部剪切与口型推理。

> **【图5-3 Wav2Lip场景流程图（占位）】**
>
> 图示说明（ASCII 占位，后续可替换为正式图）：
>
> ```text
> User_Wav / 或 VITS音频 -> Audio_save_path.json ->
> 原始视频 -> FPS标准化(25fps) -> 按页剪切 -> Wav2Lip推理 ->
> Video_save_path.json -> 后续PPT合成
> ```

工程实现上，系统通过 `User_Wav_Wav2Lip_Inference` 与 `VITS_Wav2Lip_Join.Inference_Wav2Lip` 两种入口覆盖“用户音频驱动”与“模型音频驱动”两类子场景，实现路径统一与能力复用。

### 5.3.2 场景B：SadTalker 人像驱动流程

SadTalker 场景适用于“数字人讲解”“虚拟播报”等强调表情自然度与整体观感的任务。该流程通常包含两个阶段：

1. 通过 VITS 依据 PPT 备注文本批量合成语音；
2. 将语音与人像输入 SadTalker 逐页生成视频片段。

> **【图5-4 SadTalker场景流程图（占位）】**
>
> 图示说明（ASCII 占位，后续可替换为正式图）：
>
> ```text
> Ref_Wav + Ref_Text + PPT_Remake.json
>            |
>            v
>        VITS批量合成  ------> Audio_save_path.json
>            |
>            v
>        SadTalker逐页推理 ---> Video_save_path.json
> ```

在实现细节上，系统会对页码键值进行自然排序，保证 `Slide1、Slide2、...` 的生成顺序与 PPT 页序一致，避免出现“页码与音视频错位”的工程问题。

## 5.4 端到端业务链路与任务状态管理

前端 `VideoConfigView` 在用户点击“生成视频”后，按顺序完成：

1. 上传形象素材（图片或教师视频）；
2. 上传并解析 PPT 备注；
3. 提交参数配置；
4. 根据场景选择推理接口；
5. 轮询 `Audio_Video_Inference` 状态；
6. 生成 PPT 视频并合成最终结果；
7. 拉取质量评测结果展示。

> **【图5-5 端到端时序图（占位）】**
>
> 图示说明（ASCII 占位，后续可替换为正式图）：
>
> ```text
> 用户 -> 前端(VideoConfigView) -> 后端(Get_Inference)
>      <- 返回任务标识
> 前端 -> 后端(Get_State) 循环轮询
>      <- Processing / True / Failed
> 前端 -> 后端(Generate_PPT_Video + Merge)
> 前端 -> 后端(Get_Video_Quality_Eval)
>      <- 五维评分 + 总分 + 建议
> ```

这一流程使系统具备“任务可观察、异常可定位、链路可回溯”的工程可维护性。

## 5.5 质量评测闭环：从生成到优化

与传统“只生成不评估”的数字人系统不同，本系统在生成完成后自动触发质量评测接口 `/Get_Video_Quality_Eval`，返回结构化结果：

- 总分与等级（A+：score≥92.00，A：88.00≤score<92.00，B+：82.00≤score<88.00，B：75.00≤score<82.00，C：score<75.00；边界值按“高档优先”划分，例如 score=88.00 归入 A）；
- 五维指标：口型同步、画面清晰度、时序稳定性、音频质量、表情自然度；
- 评测执行链路耗时；
- 面向薄弱维度的优化建议。

> **【图5-6 质量评测结果面板示意图（占位）】**
>
> 图示说明（ASCII 占位，后续可替换为正式图）：
>
> ```text
> +------------------------------------------------------+
> | 总分: 89.40   等级: A   Trace: xxxxxxxx              |
> | lip_sync: 91  visual: 88  temporal: 84  audio: 92 ...|
> | 执行链路: prepare -> extract -> syncnet -> aggregate |
> | 优化建议: 提升时序稳定性，建议减小表情幅度...         |
> +------------------------------------------------------+
> ```

为增强闭环价值，后端在评测阶段根据指标阈值自动输出针对性建议。例如：

- 当 `lip_sync` 偏低时，建议更换更清晰人声或优化驱动音频；
- 当 `visual_clarity` 偏低时，建议提升输入分辨率并启用增强；
- 当 `temporal_stability` 偏低时，建议降低表情幅度并检查帧率一致性。

由此形成“生成—评测—建议—再生成”的优化循环。

> **【图5-7 生成-评测-优化闭环图（占位）】**
>
> 图示说明（ASCII 占位，后续可替换为正式图）：
>
> ```text
> [输入素材/配置] -> [Wav2Lip或SadTalker生成] -> [质量评测]
>          ^                                      |
>          |                                      v
>      [参数调优与素材替换] <- [问题定位与优化建议输出]
> ```

## 5.6 系统应用价值与论文工作的结合

本研究的贡献不仅在于提出了质量评估算法，也在于将生成模型与评估机制真正集成到一套可运行系统中，具体体现在：

1. **双场景能力落地**：将 Wav2Lip 与 SadTalker 的能力差异转化为可配置、可切换的应用场景；
2. **统一工程编排**：通过统一任务接口和状态查询机制，降低多模型系统的使用复杂度；
3. **评估结果可行动**：评测输出不止“分数”，还给出可执行的优化方向，提升迭代效率；
4. **研究闭环完整**：实现从“模型研究”到“系统应用”再到“质量反馈”的闭环验证。

因此，本章将第3章“算法能力”和第4章“评估验证”与实际系统应用有效连接，支撑论文从方法创新走向工程可用。

## 5.7 本章小结

本章围绕数字人生成系统的工程实现，系统阐述了前后端架构、双场景任务分流、异步状态管理与质量评测闭环机制。通过将 Wav2Lip 与 SadTalker 组织为两类互补应用场景，并在生成流程中嵌入多维质量评测与自动建议机制，系统实现了“可生成、可评价、可优化”的完整应用链路。

该章节内容进一步证明：本论文工作不仅具备算法层面的研究意义，也具备面向真实业务场景的工程落地价值。
